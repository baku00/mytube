function like(id) {
  // var btn_like = document.getElementById('btn-like')
  // if(btn_like.className.baseVal.includes('btn-like'))
  //   btn_like.className.baseVal = btn_like.className.baseVal.replace('btn-like','')
  // else
  //   btn_like.className.baseVal += ' btn-like'
  //
  //
  // var form_datas = new FormData(form_like);
  // var _token = document.getElementsByName('_token')[0].value
  // var action = document.getElementById('form_like').action
  // var method = document.getElementById('form_like_method').value
  // var video_id = document.getElementById('video_id').value
  // var like = btn_like.className.baseVal.includes('btn-like')
  // var dislike = 0
  //
  // var http = new XMLHttpRequest()
  // var datas = '_token='+_token+'&video_id='+video_id+'&like='+like+'&dislike='+dislike
  // http.open('PUT',action)
  // console.log(datas);
  // http.send(datas)
}

function dislike(id) {
  // var btn_dislike = document.getElementById('btn-dislike')
  // if(btn_dislike.className.baseVal.includes('btn-dislike'))
  // btn_dislike.className.baseVal = btn_dislike.className.baseVal.replace('btn-dislike','')
  // else
  // btn_dislike.className.baseVal += ' btn-dislike'
}
