const NUMBER = 8

window.addEventListener('scroll', callVideos);

function callVideos() {
  // var http = new XMLHttpRequest();
  // http.open('get','http://mytube.local:8080/callVideo/3')
  // http.onload = function(videos){
  //   videos = JSON.parse(http.response)
  //   for (var i = 0; i < videos.length; i++) {
  //     var video = videos[i]
  //     var video_card = createVideoCard(
  //       'storage/videos/'+video.src+'/miniature.jpg',
  //       video.title,
  //       video.id,
  //       3, // video.comment_number
  //       4, // video.like_number
  //       5, // video.dislike_number
  //     )
  //     document.getElementById('videos').append(video_card)
  //   }
  // }
  // http.send()
}
